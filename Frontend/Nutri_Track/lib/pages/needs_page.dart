import 'package:flutter/material.dart';

class NeedsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Needs')),
      body: Center(child: Text('Needs Page Content')),
    );
  }
}
